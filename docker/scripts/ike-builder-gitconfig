[user]
	name = Alien Ike
	email = arquillian-team@lists.jboss.org
[alias]
	pr  = "!`# Checks out PR to its own branch`; \
			f() { \
				git fetch -fu ${2:-$(git remote | grep ^upstream || echo origin)} refs/pull/$1/head:pr/$1 && git checkout pr/$1; \
			}; \
			f"
			